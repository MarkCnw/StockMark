import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primaryBlue = Color(0xFF2B6BE5);
  static const primaryBlueDark = Color(0xFF64B5F6);

  // Background Colors
  static const lightBg = Color(0xFFF4F8FC);
  static const darkBg = Color(0xFF0F1115);

  // Card Colors
  static const lightCard = Colors.white;
  static const darkCard = Color(0xFF1E2128);

  // Text Colors
  static const lightTextPrimary = Colors.black87;
  static const lightTextSecondary = Color(0xFF757575);
  static const darkTextPrimary = Colors.white;
  static const darkTextSecondary = Color(0xFFB0B0B0);

  // Tab Colors
  static const lightTabUnselected = Color(0xFFE0E0E0);
  static const darkTabUnselected = Color(0xFF2A2D35);

  // Avatar/Icon Background
  static const lightIconBg = Color(0xFFE0E0E0);
  static const darkIconBg = Color(0xFF2A2D35);

  // Shadow Colors
  static const lightShadow = Color(0x1A000000);
  static const darkShadow = Color(0x00000000); // No shadow in dark mode
}